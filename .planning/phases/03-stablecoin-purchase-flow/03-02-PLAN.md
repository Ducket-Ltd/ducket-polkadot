---
phase: 03-stablecoin-purchase-flow
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: []
autonomous: false
requirements: [FEND-02, STBL-01, STBL-02, STBL-03]

must_haves:
  truths:
    - "User sees DOT/USDC payment selector on Event page"
    - "Selecting USDC updates total to show dollar amount"
    - "USDC purchase shows approve step then purchase step in sequence"
    - "Completed purchase navigates to My Tickets page"
  artifacts: []
  key_links: []
---

<objective>
Verify the stablecoin purchase flow works end-to-end in the browser on the dev server.

Purpose: Confirm all four phase requirements (FEND-02, STBL-01, STBL-02, STBL-03) are met with real UI interaction.
Output: Human-verified approval of the purchase flow.
</objective>

<execution_context>
@/Users/justinsoon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsoon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-stablecoin-purchase-flow/03-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify stablecoin purchase flow in browser</name>
  <files></files>
  <action>
Human verification of the complete stablecoin purchase flow built in Plan 01.

What was built: usePurchaseTicket hook with approve+purchase state machine, DOT/USDC payment selector, step indicator UI, error handling with retry, and success navigation.

Verification steps:
1. Run `cd frontend && npm run dev` and open the app in browser
2. Connect your wallet
3. Navigate to any event page
4. Select a ticket tier — verify DOT and USDC buttons appear below quantity selector
5. STBL-03 check: Click "USDC" — verify total changes from "X DOT" to "$X USDC"
6. STBL-02 check: Verify tier cards still show both DOT and USDC prices (already working from Phase 2)
7. STBL-01 check (if testnet contract is deployed): Click "Purchase Tickets" with USDC selected — verify "Step 1/2: Approve USDC" appears, then after wallet approval "Step 2/2: Purchase Ticket" appears
8. FEND-02 check (if testnet contract is deployed): Switch to DOT, purchase — verify single-step flow (no approve step)
9. Verify error state: reject the wallet prompt — verify red error box appears with "Try again" link
10. Verify success state: after successful purchase, verify "Purchase Complete!" shows briefly then navigates to /my-tickets

Note: Steps 7-8 require the contract to be deployed on testnet (Phase 1 CONT-06). If not yet deployed, verify steps 1-6 (UI behavior) and confirm the hook wiring compiles and renders correctly.
  </action>
  <verify>Human approval</verify>
  <done>User types "approved" confirming the purchase flow works as expected</done>
</task>

</tasks>

<verification>
Human confirms the purchase flow UI works as expected in the browser.
</verification>

<success_criteria>
- Payment selector renders and toggles total display correctly
- Step indicator shows correct labels during USDC flow
- Error handling renders on wallet rejection
- All four requirements visually confirmed (FEND-02, STBL-01, STBL-02, STBL-03)
</success_criteria>

<output>
After completion, create `.planning/phases/03-stablecoin-purchase-flow/03-02-SUMMARY.md`
</output>
