---
phase: 01-contract-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - contracts/contracts/MockUSDC.sol
  - contracts/test/MockUSDC.test.ts
autonomous: true
requirements:
  - CONT-01

must_haves:
  truths:
    - "MockUSDC deploys successfully and reports 6 decimals"
    - "Any wallet can call faucet() and receive up to 10,000 USDC"
    - "faucet() reverts when requesting more than 10,000 USDC"
    - "Deployer receives 1,000,000 USDC on construction"
  artifacts:
    - path: "contracts/contracts/MockUSDC.sol"
      provides: "ERC-20 mock stablecoin with 6 decimals and public faucet"
      contains: "function faucet"
    - path: "contracts/test/MockUSDC.test.ts"
      provides: "Unit tests for MockUSDC deployment, decimals, faucet"
      min_lines: 30
  key_links:
    - from: "contracts/test/MockUSDC.test.ts"
      to: "contracts/contracts/MockUSDC.sol"
      via: "ethers.getContractFactory('MockUSDC')"
      pattern: "getContractFactory.*MockUSDC"
---

<objective>
Create MockUSDC ERC-20 contract with 6 decimals and public faucet, verified by unit tests.

Purpose: MockUSDC is the stablecoin that DucketTickets will accept for ticket payments. It must exist and be tested before DucketTickets can reference it.
Output: MockUSDC.sol contract and passing test suite.
</objective>

<execution_context>
@/Users/justinsoon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justinsoon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contract-foundation/01-RESEARCH.md
@.planning/phases/01-contract-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MockUSDC contract and tests (TDD)</name>
  <files>contracts/contracts/MockUSDC.sol, contracts/test/MockUSDC.test.ts</files>
  <behavior>
    - Test: MockUSDC deploys without error
    - Test: decimals() returns 6
    - Test: deployer balance is 1,000,000 * 10^6 after construction
    - Test: name() returns "USD Coin (Mock)" and symbol() returns "USDC"
    - Test: faucet(1000 * 10^6) mints 1000 USDC to caller
    - Test: faucet(10001 * 10^6) reverts with "Max 10,000 USDC per faucet"
    - Test: two different accounts can each call faucet independently
  </behavior>
  <action>
RED phase: Create `contracts/test/MockUSDC.test.ts` with all tests above. Tests import ethers from hardhat. Deploy MockUSDC in beforeEach. Use `expect(...).to.equal(...)` and `expect(...).to.be.revertedWith(...)` patterns from chai. Run tests -- they must all fail (MockUSDC.sol does not exist yet).

GREEN phase: Create `contracts/contracts/MockUSDC.sol`:
- Import `@openzeppelin/contracts/token/ERC20/ERC20.sol`
- Inherit ERC20, constructor mints 1_000_000 * 10**6 to msg.sender
- Override `decimals()` to return 6
- Add `faucet(uint256 amount)` external function: require amount <= 10_000 * 10**6, then _mint to msg.sender
- SPDX-License-Identifier: MIT, pragma solidity ^0.8.24

Run tests -- all must pass.

REFACTOR: No refactoring expected -- contract is minimal.
  </action>
  <verify>
    <automated>cd /Users/justinsoon/Desktop/others/ducket-polkadot/contracts && npx hardhat test test/MockUSDC.test.ts</automated>
  </verify>
  <done>MockUSDC.sol compiles, all 7 test cases pass, contract has 6 decimals and working faucet with 10k cap</done>
</task>

</tasks>

<verification>
cd /Users/justinsoon/Desktop/others/ducket-polkadot/contracts && npx hardhat test test/MockUSDC.test.ts
All tests green.
</verification>

<success_criteria>
- MockUSDC.sol exists with ERC20 inheritance, 6 decimals, public faucet with 10k cap
- Test file covers deployment, decimals, faucet success, faucet revert, initial supply
- `npx hardhat test test/MockUSDC.test.ts` passes all tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-foundation/01-01-SUMMARY.md`
</output>
